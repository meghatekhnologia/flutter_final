import 'package:flutter/material.dart';

class MyHelp extends StatelessWidget {
  const MyHelp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar:AppBar(title:Text('Help Screen')),

    );
  }
}
